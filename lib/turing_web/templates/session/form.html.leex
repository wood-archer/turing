<%= f = form_for @changeset,
    "#",
    [
      phx_change: :validate,
      phx_submit: :sign_in
    ]
  %>

  <%= label f, :email %>
  <%= text_input f, :email %>
  <%= error_tag f, :email %>

  <%= label f, :password %>
  <%= live_view_password_input f, :password %>
  <%= error_tag f, :password %>

  <%# submit %>
  <div>
    <%= submit "Sign In", phx_disable_with: "Loading ..." %>
  </div>

</form>
